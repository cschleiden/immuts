{
  "name": "immu-ts",
  "version": "0.0.5",
  "description": "",
  "main": "dist/immuts.min.js",
  "typings": "dist/immuts.d.ts",
  "scripts": {
    "prepublish": "npm run clean && npm run build",
    "test": "npm run clean && tsc && karma start --single-run",
    "build": "webpack --debug && webpack -p && dts-bundle --name immuts --main build/main.d.ts --outputAsModuleFolder --out ../dist/immuts.d.ts",
    "clean": "rimraf build && rimraf dist"
  },
  "author": "cschleiden",
  "license": "MIT",
  "devDependencies": {
    "chai": "^3.5.0",
    "dts-bundle": "^0.5.0",
    "ifdef-loader": "^1.0.2",
    "immutable": "^3.8.1",
    "karma": "^1.1.2",
    "karma-chai": "^0.1.0",
    "karma-chrome-launcher": "^1.0.1",
    "karma-mocha": "^1.1.1",
    "karma-phantomjs-launcher": "^1.0.1",
    "karma-requirejs": "^1.0.0",
    "mocha": "^3.0.2",
    "requirejs": "^2.2.0",
    "rimraf": "^2.5.4",
    "ts-loader": "^0.8.2",
    "typescript": "^1.8.10",
    "typings": "^1.3.2",
    "webpack": "^1.13.2"
  }
}
